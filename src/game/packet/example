package game.packet.yourpackage;

import game.GameClient;
import game.packet.handler.Packet;
import game.packet.handler.PacketParser;

@Packet("your packet only 2 letters")
public class yournameclass implements PacketParser {

	@Override
	public void parse(GameClient client, String packet) {
		//yourfunction
	}
}